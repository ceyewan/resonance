# ============================================================================
# Gateway 服务 - 生产环境覆盖配置
# 说明：通过 RESONANCE_ENV=prod 启用，覆盖默认的本地配置
# ============================================================================

service:
  host: ""

redis:
  addr: resonance-redis:6379

etcd:
  endpoints:
    - resonance-etcd:2379
