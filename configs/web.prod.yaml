# ============================================================================
# Web 静态站点 - Docker 环境覆盖配置
# 用途：make up / 生产环境 - Docker 容器运行
# 说明：通过 RESONANCE_ENV=prod 启用
# ============================================================================

# 日志配置：生产环境使用 JSON 格式，方便日志收集
log:
  level: info
  format: json
  output: stdout

service:
  host: 0.0.0.0
  http_port: 4173
