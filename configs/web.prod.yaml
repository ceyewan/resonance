# ============================================================================
# Web 静态站点 - 生产环境覆盖配置
# ============================================================================

service:
  host: 0.0.0.0
  http_port: 4173
