# ============================================================================
# Gateway 开发环境配置覆盖
# ============================================================================

log:
  level: debug
  format: console

# 开发环境使用本地地址
redis:
  addr: 127.0.0.1:6379

nats:
  url: nats://127.0.0.1:4222

registry:
  namespace: /resonance/services
